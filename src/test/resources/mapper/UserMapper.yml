namespace: cat.wars.ompf.dao.UserMapper
statement:
  -
    id: findList
    type: select
    sql: "SELECT id,name FROM user WHERE id = #{id} AND name = #{name}"
    parameterType: cat.wars.ompf.model.User
    resultType: cat.wars.ompf.model.User
